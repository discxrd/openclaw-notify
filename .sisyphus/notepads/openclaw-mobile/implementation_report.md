### 3. Pairing UI
- **Server URL**: Persisted input field (defaults to emulator host).
- **Device Identity**: Displays unique Device ID and FCM Token.
- **Config Generator**: One-tap copy of Markdown configuration for easy sharing.
- **Registration**: Manual trigger to register device with the bridge server.
